<h1>Listing albums</h1>
only favorites: <%= check_box_tag :favorites_switch %>
<p></p>
<table>
  <tr>
    <th>Album</th>
    <th>Favorite</th>
    <th>Artist</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @albums.each do |album| %>
  <tr>
    <td><%= album.album_name %></td>
    <td><%= album.favorite %></td>
    <td><%= Artist.find(album.artist_id).artist_name %></td>
    <td><%= link_to 'Show', album %></td>
    <td><%= link_to 'Edit', edit_album_path(album) %></td>
    <td><%= link_to 'Destroy', album, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Album', new_album_path %>
<p></p>
<%= link_to 'View Artists', artists_path %>
<p></p>
<%= link_to 'View Songs', songs_path %>
<p></p>
<%= link_to 'Search all', get_query_path %>
